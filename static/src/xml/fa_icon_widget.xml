<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">

    <!-- Main Field Template -->
    <t t-name="fa_icon_widget.FaIconField" owl="1">
        <div class="o_field_fa_icon" t-att-class="{'o_field_readonly': props.readonly}">
            <!-- Display the selected icon -->
            <t t-set="html_icon" t-value="iconHtml()" />
            <div class="o_field_fa_icon_preview" t-if="props.value">
                <span t-out="html_icon"></span>
            </div>
        </div>

        <div class="o_field_image" aria-atomic="true">
            <t t-if="props.mode !== 'readonly'">
                <div class="o_form_image_controls">
                    <button class="fa fa-pencil fa-lg float-left fieldIconSelect" t-on-click="openIconDialog" title="Seleccionar Ícono" aria-label="Editar"/>
                    <button class="fa fa-trash-o fa-lg float-right o_clear_file_button" t-on-click="clearIcon" title="Limpiar Ícono" aria-label="Limpiar"/>
                </div>
            </t>
        </div>
    </t>

    <!-- Icon Selection Dialog Template -->
    <t t-name="fa_icon_widget.IconSelectionDialog" owl="1">
        <Dialog title="'Seleccionar Ícono'" size="'xl'">
            <div class="o_fa_icon_selection_dialog">
                <!-- Search bar -->
                <div class="o_fa_icon_search mb-3">
                    <div class="input-group">
                        <input
                            type="search"
                            class="form-control"
                            placeholder="Buscar íconos o ingresar nombre (ej. 'dollar', 'line-chart', 'bar-chart')..."
                            t-on-input="onSearch"
                            t-ref="searchInput"
                        />
                        <button class="btn btn-primary" t-on-click="onAddCustomIcon">
                            <i class="fa fa-plus me-1"></i>Personalizado
                        </button>
                    </div>
                    <small class="form-text text-muted">
                        Para ver el catálogo completo de íconos compatibles, consulta <a href="https://fontawesome.com/v4/icons/" target="_blank">FontAwesome v4 Icons</a>
                    </small>
                </div>

                <div class="o_fa_icon_grid" style="display: grid; grid-template-columns: repeat(auto-fill, minmax(100px, 1fr)); gap: 8px;">
                    <t t-foreach="state.filteredIcons" t-as="icon" t-key="icon">
                        <div
                            class="o_fa_icon_item p-2 text-center border rounded"
                            t-att-class="{'o_fa_icon_selected bg-primary text-white': state.selectedIcon === icon}"
                            t-on-click="() => this.onIconClick(icon)"
                            style="cursor: pointer;"
                        >
                            <i t-attf-class="fa fa-{{icon}} fa-2x mb-1"></i>
                            <div class="o_fa_icon_name">
                                <small t-out="icon"></small>
                            </div>
                        </div>
                    </t>
                    <t t-if="state.filteredIcons.length === 0">
                        <div class="alert alert-info w-100 text-center">
                            No se encontraron íconos con ese término. Puedes ingresar uno personalizado con el botón superior.
                        </div>
                    </t>
                </div>
            </div>
            <t t-set-slot="footer">
                <button class="btn btn-primary" t-on-click="onSelectClick" t-att-disabled="!state.selectedIcon">
                    Seleccionar
                </button>
                <button class="btn btn-secondary" t-on-click="props.close">
                    Cancelar
                </button>
            </t>
        </Dialog>
    </t>
</templates>
