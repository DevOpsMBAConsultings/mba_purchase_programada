<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="dashboard_access_view" model="ir.ui.view">
        <field name="name">dashboard.access.form.view</field>
        <field name="model">dashboard.access</field>
        <field name="arch" type="xml">
        <form string="Gestión de Acceso a Tableros">
            <group>
                <field name="is_remove" string="Revocar Acceso" widget="boolean_toggle"/>
                <field name="access_by" string="Tipo de Acceso" required="not is_remove" invisible="is_remove"/>
                <field name="group_ids" string="Grupos de Seguridad" widget="many2many_tags" invisible="is_remove or access_by != 'access_group'" required="access_by == 'access_group'" options="{'no_create': True, 'no_create_edit':True, 'no_open': True}" />
                <field name="user_ids" string="Usuarios" force_save="1" widget="many2many_tags" domain="['|', ('id', '!=', uid), ('share', '=', False)]" required="not is_remove" context="{'user_group_ids': group_ids, 'access_by': access_by, 'user_ids': True}" options="{'no_create': True, 'no_create_edit':True, 'no_open': True}" invisible="is_remove"/>
                <field name="dashboard_id" invisible="1" options="{'no_create': True, 'no_create_edit':True, 'no_open': True}"/>
            </group>
            <footer>
                <button string="Confirmar" name="action_confirm" type="object" default_focus="1" class="btn-primary" data-hotkey="q"/>
                <button string="Cancelar" class="btn-secondary" special="cancel" data-hotkey="z"/>
            </footer>
        </form>
        </field>
    </record>

    <record id="action_dashboard_access" model="ir.actions.act_window">
        <field name="name">Gestión de Acceso a Tableros</field>
        <field name="res_model">dashboard.access</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
        <field name="binding_view_types">list</field>
    </record>

</odoo>
